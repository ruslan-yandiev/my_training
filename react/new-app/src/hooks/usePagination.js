// import { useMemo } from "react";
// import {getPageCount, getPagesArray} from '../utils/pages'

// export function usePagination(totalPages, totalCount, limit) {
//   const pagesArray = useMemo(() => getPagesArray(totalPages), [totalPages]);
//   const pageCount = useMemo(() => getPageCount(totalCount, limit), [totalCount, limit]);

//   return [pagesArray, pageCount];
// }